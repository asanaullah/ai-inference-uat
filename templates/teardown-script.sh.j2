#!/bin/bash
# Assisted by Claude Opus 4.6
set -e
echo "Tearing down resources with selector: {{ selector }}"
oc delete {{ resource_types }} -l {{ selector }} --ignore-not-found -n {{ namespace }}
echo "Teardown complete"
