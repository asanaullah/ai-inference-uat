# Assisted by Claude Opus 4.6
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: uat-models
  namespace: uat-project
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: nfs-csi
  resources:
    requests:
      storage: 500Gi
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: uat-models
  namespace: uat-peer
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: nfs-csi
  resources:
    requests:
      storage: 500Gi
